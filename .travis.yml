language: python
python:
  - '3.6'
  - '3.7'

install: pip install tox-travis

script: tox

after_success:
  - codecov

deploy:
  provider: pypi
  user: "paulgessinger"
  on:
      tags: true
  skip_existing: true
  distributions: "sdist bdist_wheel"
